<Project>

  <!-- Intentionally empty. Historically pinned v8 Microsoft.Extensions / System.Text.Json
       packages to work around a version conflict introduced by a since-removed
       protocol dependency; with that dependency gone, transitive resolution is
       coherent without pins. -->

</Project>
